/* Crea una función addFivesin argumentos. Esta función suma 5 a la sumvariable, pero su valor devuelto es undefined.let sum = 0;
1-addFivedebe ser una función.
2-Una vez que se hayan ejecutado ambas funciones, sumdebería ser igual a 8.
3-El valor devuelto de addFivedebe ser undefined.
4-Dentro de la addFivefunción, debe agregar 5a la sumvariable. */

function addThree() {
  sum = sum + 3;
}

function addFive(){
  sum = sum + 5;
}

addThree();
addFive();