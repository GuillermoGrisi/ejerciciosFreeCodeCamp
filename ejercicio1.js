/* Cree una función timesFiveque acepte un argumento, lo multiplique por 5y devuelva el nuevo valor.
1-timesFive(5)debería volver25
2-timesFive(2)debería volver10
3-timesFive(0)debería volver0 */


function timesFive(num){
    return num * 5;
  }
  
  const resp1 = timesFive(5);
  const resp2 = timesFive(2);
  const resp3 = timesFive(0);